Sound file placeholder. The app plays /sounds/new-message.mp3 when present;
otherwise it falls back to a Web Audio beep (see websiteChatNotificationEffects.js).
Replace this file with your own new-message.mp3 asset for production.
