# Secrets & local env (keep *.env.example tracked)
.env
.env.*
!.env.example
!**/.env.example

# Dependencies & build
node_modules/
chat-server/node_modules/
dist/

# Database dumps & caches
*.sql
.phpunit.result.cache

# Ephemeral dev scripts (wait-laravel / wait-chat / serve helpers)
scripts/.laravel-bind-port
scripts/.laravel-active-port
scripts/.chat-active-port
scripts/.laravel-start-status.json

# Local noise (archives, nested copy of repo, runtime logs)
*.rar
logs/
amalgated_lendingV2/
